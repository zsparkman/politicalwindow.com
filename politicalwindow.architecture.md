# politicalwindow.com — Architecture

_Last generated: 2026-04-24_
_Generated by: Claude Code (Opus)_
_Source of truth: this repository at commit `7aefc0d`_

## 1. Purpose

The user-facing web frontend for Political Window — a political-advertising
intelligence product. Static HTML/CSS/JS hosted on GitHub Pages at
`politicalwindow.com`. It is a **multi-page** static site (not a SPA in the
routing sense) whose pages share a common style sheet and pull live data at
load time from `politicalwindow-api` (candidate / state / ballot / poll data)
and `ratewindow-api` (FCC ad-rate intelligence, candidate-tracker spend, LUR
analysis, PDF proxying, auth).

> **Inconsistency:** `CLAUDE.md` (this repo) describes the frontend as a
> "single file: `index.html` contains all HTML, CSS, and JavaScript inline"
> and rule #1 reads "`index.html` stays a single self-contained file — no
> external JS or CSS files." The committed tree contradicts both: it
> contains seven HTML pages and a shared `style.css`. Treat the
> committed tree as the source of truth and CLAUDE.md as out of date on
> this point.

## 2. Tech Stack

- **Runtime:** none on the server — pure static hosting.
- **Build step:** none. No `package.json`, no bundler, no transpiler. Files
  are served verbatim by GitHub Pages.
- **CDN-loaded libraries** (referenced from `index.html`):
  - **MapLibre GL JS 4.7.1** (unpkg) — vector basemap rendering. Falls back
    to a hand-rolled tile-grid cartogram if the CDN fails (CLAUDE.md
    "External CDN Dependencies").
  - **CARTO Dark Matter** basemap tiles (no API key).
- **Fonts:** Bebas Neue (display), IBM Plex Mono (data), DM Sans (body) —
  loaded via Google Fonts.
- **Hosting:** GitHub Pages with custom domain. `CNAME` contains
  `politicalwindow.com`. No `.github/workflows/` directory exists, so
  deployment uses GitHub Pages' default "Deploy from branch" mode (push
  to the configured branch → published).

## 3. Entry Points and Boot Sequence

There is no shared entry point. Each top-level page is a standalone HTML
document that links to `style.css` and contains its own JS. The flagship
page is `index.html` (the map + state-detail experience). On boot,
`index.html`:

1. Defines hard-coded API base URLs at `index.html:4035-4036`:
   - `API_BASE  = 'https://api.politicalwindow.com'`
   - `RATES_API = 'https://rates.politicalwindow.com'`
2. Calls `loadLiveData()` (defined per CLAUDE.md function table) which
   fans out parallel `fetch()` calls (`index.html:4042-4045`):
   - `${API_BASE}/api/states`
   - `${API_BASE}/api/candidates`
   - `${API_BASE}/api/ballot-measures`
   - `${API_BASE}/api/polls`
3. Populates the runtime data structures (`SM`, `CANDS`, `BALLOT`, `POLLS`
   — see CLAUDE.md "Data Structures").
4. Pulls a US-states GeoJSON from a third-party raw GitHub URL
   (`index.html:1457`: `raw.githubusercontent.com/PublicaMundi/MappingAPI`).
5. Initializes MapLibre via `initMap()`; on CDN failure falls back to the
   tile-grid `buildMap()` rendering.
6. Wires per-state interactions (`pick()`, `mapSelectState()`,
   `wireDetailTabs()`) and the embedded login overlay (see Section 8).

Other pages (`candidate-tracker.html`, `lur.html`, `rates.html`,
`explorer.html`, `public-file.html`, `admin.html`) follow the same pattern:
inline JS, hit one or both APIs, render.

## 4. Directory Layout

```
politicalwindow.com/
  index.html               Map / state-detail experience (the home page; ~221 KB)
  candidate-tracker.html   Candidate-spend explorer (filters by office/party/state)
  rates.html               Rate-intel browser (ratewindow-api /api/rates)
  public-file.html         FCC public-file viewer (ratewindow-api /api/public-file)
  lur.html                 Lowest-Unit-Rate violations + invoice review modal
  explorer.html            Pivot-style line-item explorer (ratewindow-api /api/lines)
  admin.html               Admin console (invites, users, API keys)
  coastal.html             Coastal TV Owner Dashboard — dedicated per-owner viewport at /coastal (added 5/12/26, supersedes the short-lived groups.html)
  style.css                Shared CSS variables, layout, design tokens
  CNAME                    `politicalwindow.com` (GitHub Pages custom domain)
  TOKENS.md                Design-token reference (colors, spacing, typography)
  CHANGES.md               Rolling change log
  CLAUDE.md                Operating guide for Claude Code in this repo (note: out of date re: "single file")
  README.md                Brief repo readme
  Design Reference/        Static reference screenshots / mockups
```

There are no JS bundles, no `dist/`, no `node_modules/`. JS is inline in
each `*.html` `<script>` block.

## 5. Data Model

No direct database access. The frontend's "data model" is the in-memory
shape that `loadLiveData()` produces from API responses
(`index.html:4035-...`, see CLAUDE.md "Data Structures"):

- `SM` — `{ TX: { pWindow, rWindow, dWin, dPrim, dRun, dRunW, status }, ... }`
  (state metadata; status drives map color)
- `CANDS` — `{ TX: [{ race, list:[{n, p, s, note, d}] }] }` (candidates by
  state; `d` is the House district number)
- `BALLOT` — ballot measures grouped by state (from `/api/ballot-measures`)
- `POLLS` — `{ [state_abbr]: { [race]: [{ pollster, date, sample, moe, results }] } }`
- `glMap`, `statesGeoJSON`, `DISTRICT_CACHE`, `_financeCache`,
  `candTabState`, `_currentStateAbbr`, `_hoveredDistrictId`,
  `DISTRICT_COUNTS` — UI-state caches; see CLAUDE.md for full descriptions.

CLAUDE.md rule #3 forbids `localStorage`, `sessionStorage`, and cookies.

> **Inconsistency:** the embedded mini-login overlay at the bottom of
> `index.html:4434` violates that rule — on a successful `POST /auth/login`
> it calls `localStorage.setItem('pw_token', res.d.token)` and
> `localStorage.setItem('pw_user', res.d.email)`. Either the rule is
> stale or this code is. Flag for the maintainer.

## 6. External Integrations

| Service | Base URL | Purpose | Auth |
|---|---|---|---|
| `politicalwindow-api` | `https://api.politicalwindow.com` | States, candidates, ballot measures, polls | none (public) |
| `ratewindow-api` | `https://rates.politicalwindow.com` | FCC rate intel, candidate-tracker spend, FEC data, LUR, PDFs, auth | JWT (Bearer) for pro-tier endpoints; pulled from `localStorage.pw_token` after login |
| MapLibre GL JS (CDN) | unpkg.com | Vector map rendering | none |
| CARTO Dark Matter (CDN) | basemaps.cartocdn.com | Basemap tiles | none |
| PublicaMundi MappingAPI (raw GitHub) | `raw.githubusercontent.com/PublicaMundi/MappingAPI/master/data/geojson/us-states.json` | US states GeoJSON | none — fetched at `index.html:1457` |
| Google Fonts | fonts.googleapis.com | Bebas Neue / IBM Plex Mono / DM Sans | none |

Congressional district GeoJSON is fetched per state on demand (CLAUDE.md
`loadDistricts(abbr)` function); the URL pattern is hard-coded in
`index.html`. **UNKNOWN — the exact district-GeoJSON source URL is not
extracted into a constant; verify by grepping `loadDistricts` in
`index.html` if needed.**

## 7. Page / Route Map

| Page | Reads from | Notes |
|---|---|---|
| `index.html` | `politicalwindow-api`: `/api/states`, `/api/candidates`, `/api/ballot-measures`, `/api/polls`. `ratewindow-api`: `/api/candidates/tracker?state=X` (per-state enrichment), `/api/candidates/tracker/:id/fec` (batched on selection) | Map + state detail panel + Candidates / Issues / Polling / Overview tabs. Hosts the mini-login overlay. |
| `candidate-tracker.html` | `ratewindow-api`: `/api/candidates/tracker` (default sort `total_spend DESC`) | Filterable spend leaderboard. |
| `rates.html` | `ratewindow-api`: `/api/rates`, `/api/cable-systems` | Pro-tier rate intel browser. |
| `public-file.html` | `ratewindow-api`: `/api/public-file` | Open-tier FCC public-file viewer (no auth needed). |
| `lur.html` | `ratewindow-api`: `/api/lur*` (`/api/lur`, `/api/lur-violations`, `/api/lur-monitor`, `/api/lur-campaign`, `/api/lur-slots`, `/api/lur-options`, `/api/lur-entities`), `/api/pdf-proxy/:id`, `/api/pdf-warm` | Lowest-Unit-Rate analysis + invoice-comparison modal. |
| `explorer.html` | `ratewindow-api`: `/api/lines`, `/api/lines/filters` | Line-item pivot explorer. |
| `admin.html` | `ratewindow-api`: `/api/invites`, `/api/invites/users`, `/api/keys` | Admin console; requires `admin`-tier API key. |
| `coastal.html` (served at `/coastal`) | `ratewindow-api`: `/api/invoices?state=`, `/api/lur?callsign=`, `/api/lines?station=`, plus `/auth/login`, `/auth/register`, `/auth/me`. **No new endpoints — pure read-only viewport.** | **Coastal TV** Owner Dashboard, dedicated per-owner page (the multi-tenant `groups.html` engine from 5/11 was replaced 5/12 by per-owner files — copy this to `<owner-slug>.html` and edit the `GROUP` constant at the top to onboard a new owner). Full auth required — login overlay matches the `lur.html` flow exactly (JWT in `localStorage.pw_token`). Five views via query param `?view=<hero\|markets\|market\|compliance\|competition\|exports>` plus `&dma=<DMA>` for market drill-in and `&demo=true` for competitor-callsign anonymization. Visual primitives unique to this page (none of which are reused elsewhere yet): two-card hero split with 5.5rem Bebas Neue numbers; share-of-voice scoreboard rows sized by total market $; 220×220 SVG share donut with peer-benchmark dot; gradient-fill weekly trend chart; daypart × week heatmap; compliance station-wall tiles (red exposed / green clean); 25-card advertiser leaderboard. Owner Briefing PDF generated by browser print (`@media print` stylesheet + `beforeprint` hook that appends a full `#print-briefing` block regardless of the user's current view); PPTX deferred to v2. See `CHANGES.md` 2026-05-12 for the full feature index, visual decisions, and v2 deferrals. |

CLAUDE.md "Station display convention" applies across `rates.html`,
`public-file.html`, `lur.html`, `explorer.html`, and a simplified version
in `candidate-tracker.html`: every page exposes a `stationLabel(row)` /
`stationLabelFromCode(code)` helper plus a `STATION_DISPLAY` map
populated from `GET /api/cable-systems?limit=500`. Display format is
`"<Brand> (<DMA>)"` — e.g. `Spectrum (Los Angeles)`.

## 8. Authentication and Authorization

The frontend itself has no auth; pages render and degrade gracefully on
401/403. Authentication is only required for `ratewindow-api` pro/admin
endpoints, and the JWT round-trip is owned by the **mini-login overlay**
embedded as an IIFE at the bottom of `index.html:4434`:

1. User submits email + password to `https://rates.politicalwindow.com/auth/login`.
2. On 200, the JWT and email are stored in `localStorage` (`pw_token`,
   `pw_user`) and the page reloads.
3. Subsequent fetches read `localStorage.pw_token` and send
   `Authorization: Bearer <token>`. The `ratewindow-api` auth middleware
   resolves the JWT to a synthesized `pro`-tier API-key context (see
   `ratewindow-api/middleware/auth.js`).

Registration, password change, key generation, and user disabling all
flow through `admin.html` against the admin-only `/api/invites*` and
`/api/keys` routes on `ratewindow-api`.

There is no client-side role/permission model: tier gating is enforced
server-side and the UI just hides or grays features that 403.

## 9. Background Jobs / Scheduled Tasks

None. The frontend has no background workers, no service worker, no
polling loop beyond the per-page initial fetch. CLAUDE.md does not
register any.

## 10. Configuration and Secrets

No `.env`, no build-time config, no environment variables. The only
configuration knobs are the hard-coded constants at `index.html:4035-4036`
(and equivalent constants in each sibling HTML file):

| Constant | Value | Purpose |
|---|---|---|
| `API_BASE` | `https://api.politicalwindow.com` | politicalwindow-api base URL |
| `RATES_API` | `https://rates.politicalwindow.com` | ratewindow-api base URL |
| `TODAY` | `new Date()` | Drives map status colors against today's date |
| `GENERAL` | `new Date(2026, 10, 3)` | General election date (Nov 3, 2026) |

CLAUDE.md rule #5 forbids changing `API_BASE` or `RATES_API`. There are
no secrets in this repo.

## 11. Build, Test, and Deploy

- **Build:** none. Files are committed as-is.
- **Local dev:** open `index.html` in a browser, or `python3 -m http.server`
  / equivalent. The APIs already include `localhost` in their CORS allow-lists.
- **Tests:** none. No test runner, no spec files.
- **Deploy:** GitHub Pages, custom domain via `CNAME`. No
  `.github/workflows/` is present, so this uses Pages' "Deploy from branch"
  mode. **UNKNOWN — which branch GitHub Pages serves from (likely `main`,
  but not verified from within the repo).**

## 12. Known Quirks and Gotchas

- **`index.html` is enormous** (~221 KB / ~4500+ lines of inline JS) and
  the `loadLiveData` fan-out plus MapLibre boot all run in the same
  script. Any blocking error mid-script can leave the map and detail
  panel partially wired.
- **MapLibre fallback** is a real fallback path (`buildMap()` tile-grid
  cartogram). Code that touches `glMap` must null-check.
- **`candTabState` persists per-state across re-entry** (CLAUDE.md), so
  switching away from a state and back preserves Candidates-tab UI
  selections (office, expanded parties, drilled-in district).
- **At-large districts**: only AK, DE, ND, SD, VT, WY are labelled `AL`.
  `DISTRICT_COUNTS` is the single source of truth for "is this state
  at-large?" (CLAUDE.md).
- **Active-district illumination is unconditional on House-bearing
  states** (Follow-up #2, 4/15) — every loaded CD lights up regardless
  of tracker spend coverage. `getCandidatesFor` is no longer the gate.
- **Station-display convention**: dropdown `value` keeps the raw
  callsign/PSID; only `textContent` shows the human label. Never change
  the field function in `updateOptionStates` to return the brand string.
- **Multi-page reality vs. CLAUDE.md "single file" claim** — see the
  inconsistency noted at the top of this document.
- **`localStorage` use vs. CLAUDE.md "no client-side persistence" rule**
  — see Section 5.

## 13. Open Questions for the Maintainer

1. Should CLAUDE.md be updated to reflect the multi-page reality, or is
   the long-term plan to consolidate back into a single file?
2. Is the `localStorage` JWT storage in `index.html:4434` intentional, or
   should it be replaced (e.g., httpOnly cookie issued by the API)?
3. Where is the congressional-district GeoJSON hosted? It's fetched per
   state by `loadDistricts(abbr)` but the URL pattern isn't called out
   in CLAUDE.md.
4. Is there a CI/lint/test path that's not represented by files in this
   repo (e.g., a separate `politicalwindow.com-ci` repo)?
5. Which GitHub Pages branch is configured to publish? `main`?
